import { createTheme } from '@mui/material/styles';

// ==================================================

const theme = createTheme({
  palette: {
    primary: { main: '#F0F8FF' }, // alice blue
    secondary: { main: '#E0E0E0', dark: '#616161' }, // steel
  },
});

// ==================================================

export default theme;
