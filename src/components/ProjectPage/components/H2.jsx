import { styled } from '@mui/material/styles';

// ==================================================

const H2 = styled('h2')({
  textDecorationLine: 'underline',
  textDecorationThickness: '0.1em',
});

// ==================================================

export default H2;
